Extract in REPO ROOT (file lands at app/components/FlightBoard.tsx), commit + push.
Estimat column now always shows the current expectation: gray scheduled time when on time, red when a revision differs.
The function change (forward-looking window) Claude deploys directly - nothing for you to do there.
