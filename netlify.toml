# SPA routing — serve index.html for all paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Register edge function for blog bot interception
[[edge_functions]]
  function = "og-rewrite"
  path = "/blog/*"
# Force HTTPS
[[redirects]]
  from = "http://*"
  to = "https://:splat"
  status = 301
  force = true

# Redirect www to non-www (or vice versa)
[[redirects]]
  from = "https://www.transilvaniatimes.com/*"
  to = "https://transilvaniatimes.com/:splat"
  status = 301
  force = true
